 import { StyleSheet, Text, View } from 'react-native'
 import React from 'react'
 import { Custom, Home, Settings, Splash } from './pages'
 
 const App = () => {
  return <Custom />
 }
 
 export default App
 
 const styles = StyleSheet.create({})